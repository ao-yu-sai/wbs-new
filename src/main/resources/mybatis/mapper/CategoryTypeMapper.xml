<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ai_offshore.tools.wbs.web.mapper.CategoryTypeMapper">
    
    <resultMap id="CategoryTypeResultMap" type="CategoryType">
        <id property="categoryTypeId" column="category_type_id"/>
        <result property="categoryTypeName" column="category_type_name"/>
        <result property="description" column="description"/>
        <result property="displayOrder" column="display_order"/>
        <result property="isActive" column="is_active"/>
    </resultMap>

    <select id="findAll" resultMap="CategoryTypeResultMap">
        SELECT * FROM category_type
        WHERE is_active = true
        ORDER BY display_order, category_type_id
    </select>

    <select id="findById" resultMap="CategoryTypeResultMap">
        SELECT * FROM category_type
        WHERE category_type_id = #{categoryTypeId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="categoryTypeId">
        INSERT INTO category_type (
            category_type_name,
            description,
            display_order,
            is_active
        ) VALUES (
            #{categoryTypeName},
            #{description},
            #{displayOrder},
            #{isActive}
        )
    </insert>

    <update id="update">
        UPDATE category_type
        SET category_type_name = #{categoryTypeName},
            description = #{description},
            display_order = #{displayOrder},
            is_active = #{isActive}
        WHERE category_type_id = #{categoryTypeId}
    </update>

    <delete id="delete">
        UPDATE category_type
        SET is_active = false
        WHERE category_type_id = #{categoryTypeId}
    </delete>
</mapper> 